--- src/mesh/meshterm.css
+++ src/mesh/meshterm.css
@@ -2408,7 +2408,7 @@
 	top: 0;
 	height: var(--intro-height, calc(100% / var(--intro-n, 6)));
 	display: grid;
-	grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
+	grid-template-columns: minmax(0, 560px) minmax(0, 1fr);
 	grid-template-rows: minmax(0, 1fr) auto;
 	grid-template-areas: "copy device" "progress progress";
 	align-items: stretch;
@@ -2551,13 +2551,9 @@
 	transform: none;
 }
 .meshterm .intro-device {
-	position: relative;
+	position: absolute;
-	grid-area: device;
-	z-index: 1;
-	justify-self: stretch;
-	align-self: stretch;
-	min-width: 0;
-	min-height: 0;
-	width: 100%;
-	height: 100%;
-	max-height: 100%;
+	inset: 0;
+	z-index: 0;
 	touch-action: none;
+	pointer-events: none;
 }
+.meshterm .intro-device * {
+	pointer-events: auto;
+}
