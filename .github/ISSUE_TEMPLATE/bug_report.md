---
name: Bug report
about: Something in the firmware, tooling, or web app misbehaves
title: ""
labels: bug
---

**Where**

<!-- firmware (t-deck) / simulator / host tooling (which script) / web app (which tab) -->

**What happened, and what you expected**

**Reproduce**

<!-- Steps. For parser issues, attach the capture or pointer bytes that
     trigger it — every parser has a host-side harness, so an artifact is
     the fastest path to a fix. -->

**Environment**

<!-- commit hash, host OS, device hardware if relevant -->
