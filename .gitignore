.pio/
.DS_Store
/dist/
__pycache__/
.hallmark/

# webapp
node_modules
node_modules/
webapp/dist/
webapp/.vercel/
.vercel

# Move build output
contracts/capture-registry/build/

# Agent worktrees and session transcripts — never publish these
.claude/

# Generated by scripts/embed_map_tiles.py (~8 MB of source); rebake locally.
src/assets/map_tiles_baked.cpp

# Xcode build output. DerivedData is already ignored under ios/, but agents
# building with an explicit -derivedDataPath produce differently-named copies,
# and build logs land beside them. These are megabytes of machine output that
# would otherwise arrive in a worktree merge looking like source.
ios/DerivedData*/
ios/*.log

# Swift Package Manager build output. `swift test` writes it inside the
# package, and a worktree merge brought 200+ of these in looking like source.
ios/Packages/*/.build/
